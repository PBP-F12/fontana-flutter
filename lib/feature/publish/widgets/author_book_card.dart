import 'package:flutter/material.dart';

class AuthorBookCard extends StatelessWidget {
  const AuthorBookCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
